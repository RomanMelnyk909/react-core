import BorderWrapper from '../BorderWrapper';
import styles from './products.module.css';

const Products = () => {
	return (
		<>
			<div className={styles['common']}>Products</div>
			<BorderWrapper />
		</>
	);
}

export default Products;
